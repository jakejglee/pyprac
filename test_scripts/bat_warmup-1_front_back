# Warmup-1: front_back 
# Exercise taken from codingbat.com
# by Jake Lee, 11/24
# DESCRIPTION: Given a string, return a new string where the first and last chars have been exchanged.

def front_back(str):
  if len(str) <= 1:
    return str
  strmid = str[1:len(str)-1]
  return str[len(str)-1] + strmid + str[0]

